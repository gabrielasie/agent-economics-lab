# python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# project
# LLM response cache: commit a curated sample, ignore the rest
.cache/
# captured experiment runs (traces + transcripts); local until reviewed
runs/
results/*.json
*.png
.env
# the real Streamlit key; commit .streamlit/secrets.toml.example instead
.streamlit/secrets.toml
# personal notes (interview prep etc.); kept local, never published
notes/
